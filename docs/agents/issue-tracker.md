# Issue tracker

No GitHub remote for this repo. Track work in `TASKS.md` at the repo root (create on first use).

- One section per unit of work; heading states the outcome.
- Mark state inline: `needs-triage`, `in-progress`, `done`, `wontfix`.
- Close only with evidence noted next to the item (command output, test name).
