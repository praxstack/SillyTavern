# Domain docs

Single context for this repo:

- `CONTEXT.md` at the root indexes domain vocabulary and key documents.
- Architecture decisions live in `docs/adr/` (one file per decision, numbered).

Domain summary: Third-party SillyTavern upstream clone. Keep local branches; never push to upstream.
